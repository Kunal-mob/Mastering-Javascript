const numbers = [10, 20, 30];

let [a, b, c] = numbers;

console.log(a); // 10
console.log(b); // 20
console.log(c); // 30
const person = { name: "Alice", age: 25, city: "New York" };

let { name, age } = person;  // Extracts properties

console.log(name); // Alice
console.log(age);  // 25
