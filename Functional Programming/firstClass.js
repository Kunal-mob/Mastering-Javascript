/*In JavaScript, functions are treated like variables—you can:

Assign them to variables
Pass them as arguments
Return them from other functions*/

const greet = function(name) {
    return `Hello, ${name}!`;
  };
  
  console.log(greet("Alice")); // Hello, Alice! ✅
  